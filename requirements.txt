# Pha P (Nhịp 1 - Khởi động)
# LUU Y (phat hien khi review Pha S): pin "numpy<2" ban dau dat vi IDTxl dung
# np.math.factorial (bi go o numpy 2.0). Khi cai wfdb/mne cho Pha S, numpy da TROI
# len 2.4.6 (pip nang cap numpy nhu dependency bac cau, khong bi chan boi pin nay vi
# pin chi ap dung khi cai TU dong requirements.txt, khong ap dung khi cai them goi
# moi sau do). Da xac nhan IDTxl VAN chay dung o numpy 2.4.6 (KSG/Binning/Symbolic
# deu test lai OK) nen bo pin cho khop thuc te moi truong dang dung - neu tao venv
# moi tu dau va IDTxl loi voi numpy 2.x, thu ha ve numpy<2 truoc.
numpy
scipy
pandas
PyYAML
matplotlib
seaborn
tqdm
pytest

# PyTorch — bản CPU. Nếu có GPU NVIDIA, cài riêng theo hướng dẫn tại
# https://pytorch.org/get-started/locally/ thay vì dòng dưới.
torch

# Baseline TE cổ điển (KSG / binning) — cần JVM, xem docs/PHASE_P_GUIDE.md muc 1.3
# Xac minh ten goi/API truoc khi dung, IDTxl co the da doi so voi ban duoi day.
JPype1

# Symbolic TE: chua chon thu vien co dinh — xem docs/references/symbolic_te_decision.md
# (co the them pyinform hoac tuong duong sau khi quyet dinh)

# Pha S (du lieu sinh ly that) — xem docs/PHASE_S_GUIDE.md
# wfdb: doc dinh dang PhysioNet (.dat/.hea) + tai du lieu — dung cho slpdb, fantasia,
#       apnea-ecg. mne: doc EDF — CAP Sleep Database luu duoi dang EDF.
wfdb
mne
scikit-learn  # can cho mne.preprocessing.ICA (method='fastica') - khu nhieu dien
              # tim khoi EEG capslpdb bang ICA, xem eeg.py::remove_cardiac_artifact_ica
